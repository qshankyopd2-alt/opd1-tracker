# Pinned to the exact versions this release was built and tested against, so a
# fresh install can never pull a newer, untested package. Regenerate with:
#   .venv/Scripts/python -m pip freeze > backend/requirements.txt
# (then re-add these comments), and re-test on a clean machine.

# --- direct dependencies ---
Flask==3.1.3
flask-cors==6.0.3
requests==2.34.2
python-dotenv==1.2.2
urllib3==2.7.0
valclient==2.28        # local Riot client API (career/match-history endpoints)

# --- transitive dependencies ---
# pip installs these anyway; pinned so the resolver can't swap in a new version
# underneath us. This file doubles as the constraints file for on-demand
# installs, so every version lives here and nowhere else.
blinker==1.9.0
certifi==2026.5.20
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
tzdata==2026.3
